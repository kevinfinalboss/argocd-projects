apiVersion: v1
kind: Service
metadata:
  name: {{ include "aurora.fullname" . }}
  labels:
    app: {{ include "aurora.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "aurora.name" . }}
